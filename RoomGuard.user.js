// ==UserScript==
// @name         RoomGuard 房间守卫
// @namespace    roomguard-mod
// @version      1.0.0
// @author       pulsar-catnya
// @description  房间管理员自动守房：新号进房 / 未说话就改衣服 / 未说话就上束缚道具，满足条件自动踢出并记日志，达到次数自动拉黑。聊天室输入 /rg 打开浮窗。
// @match        https://www.bondageprojects.elementfx.com/*
// @match        https://bondage-europe.com/*
// @match        https://bondageprojects.com/*
// @match        https://www.bondage-asia.com/*
// @match        https://bondage-asia.com/*
// @run-at       document-end
// @grant        none
// @downloadURL  https://pulsar-catnya.github.io/bc-roomguard/RoomGuard.user.js
// @updateURL    https://pulsar-catnya.github.io/bc-roomguard/RoomGuard.user.js
// ==/UserScript==

(() => { "use strict"; const RGNow = () => Date.now(); const RGLog = (...a) => console.log("[RoomGuard]", ...a); const RGErr = (...a) => console.error("[RoomGuard]", ...a); const RG_VERSION = "1.0.0"; const RG_STORE_KEY = "RoomGuardData"; const RG_WIN_KEY = "RoomGuardWin"; const RG_DOT_KEY = "RoomGuardDot"; const RG_LANG_KEY = "RoomGuardLang"; const RG_MIN_WIN_W = 360; const RG_MIN_WIN_H = 320; const RG_LOG_CAP = 200; const RG_ACTION_CAP = 100; const RG_SAVE_DEBOUNCE_MS = 300; const RG_DAY_MS = 86400000; const RG_SPEECH_TYPES = ["Chat"]; const RGStorage = { lastError: null, get(key) { try { return localStorage.getItem(key); } catch (e) { return null; } }, set(key, value) { try { localStorage.setItem(key, value); return true; } catch (e) { this.lastError = e; return false; } }, remove(key) { try { localStorage.removeItem(key); } catch (e) {   } }, }; function RGCurrentNum() { try { if (typeof Player !== "undefined" && Player && Number.isInteger(Player.MemberNumber) && Player.MemberNumber > 0) { return Player.MemberNumber; } } catch (e) {   } return null; } function RGCharOf(num) { try { if (typeof ChatRoomCharacter !== "undefined" && Array.isArray(ChatRoomCharacter)) { const c = ChatRoomCharacter.find(x => x && x.MemberNumber === num); if (c) return c; } if (typeof Character !== "undefined" && Array.isArray(Character)) { const c = Character.find(x => x && x.MemberNumber === num); if (c) return c; } } catch (e) {   } return null; } function RGDisplayName(num) { const c = RGCharOf(num); if (c) { if (typeof c.Nickname === "string" && c.Nickname) return c.Nickname; if (typeof c.Name === "string" && c.Name) return c.Name; } return "#" + num; } function RGGroupLabel(group) { try { if (typeof AssetGroupGet === "function" && typeof group === "string") { const g = AssetGroupGet("Female3DCG", group); if (g && typeof g.Description === "string" && g.Description && g.Description.indexOf("MISSING") !== 0) { return g.Description; } } } catch (e) {   } return group; } function RGItemLabel(group, name) { if (name == null) return ""; try { if (typeof AssetGet === "function" && typeof group === "string" && typeof name === "string") { const a = AssetGet("Female3DCG", group, name); if (a && typeof a.Description === "string" && a.Description && a.Description.indexOf("MISSING") !== 0) { return a.Description; } } } catch (e) {   } return name; } function RGDefaultState() { return { v: 1, settings: { enabled: true, condAge: true, condClothes: true, condRestraint: true, minAgeDays: 30, combine: "any", banAfterKicks: 3, clothesMaxSpoken: 1, restraintMaxSpoken: 1, notifyBeforeKick: true, msgDelaySeconds: 1.5, kickDelaySeconds: 3, msgAge: "提示：{0} 的账号创建时间过短，即将被移出房间。", msgClothes: "提示：{0} 尚未发言就开始改动他人的服装，即将被移出房间。", msgRestraint: "提示：{0} 尚未发言就给他人穿戴束缚道具，即将被移出房间。", }, kickCount: {}, banCount: {}, log: [], }; } function RGNormalizeState(raw) { const d = RGDefaultState(); if (raw && typeof raw === "object") { const s = raw.settings || {}; const st = d.settings; st.enabled = s.enabled !== false; st.condAge = s.condAge !== false; st.condClothes = s.condClothes !== false; st.condRestraint = s.condRestraint !== false; st.minAgeDays = Number.isFinite(Number(s.minAgeDays)) && Number(s.minAgeDays) > 0 ? Math.floor(Number(s.minAgeDays)) : 30; st.combine = (s.combine === "all" || s.combine === "two") ? s.combine : "any"; st.banAfterKicks = Number.isFinite(Number(s.banAfterKicks)) && Number(s.banAfterKicks) >= 0 ? Math.floor(Number(s.banAfterKicks)) : 3; st.clothesMaxSpoken = Number.isFinite(Number(s.clothesMaxSpoken)) && Number(s.clothesMaxSpoken) >= 1 ? Math.floor(Number(s.clothesMaxSpoken)) : 1; st.restraintMaxSpoken = Number.isFinite(Number(s.restraintMaxSpoken)) && Number(s.restraintMaxSpoken) >= 1 ? Math.floor(Number(s.restraintMaxSpoken)) : 1; st.notifyBeforeKick = s.notifyBeforeKick !== false; st.msgDelaySeconds = Number.isFinite(Number(s.msgDelaySeconds)) && Number(s.msgDelaySeconds) >= 0 ? Number(s.msgDelaySeconds) : 1.5; st.kickDelaySeconds = Number.isFinite(Number(s.kickDelaySeconds)) && Number(s.kickDelaySeconds) >= 0 ? Number(s.kickDelaySeconds) : 3; st.msgAge = typeof s.msgAge === "string" ? s.msgAge : st.msgAge; st.msgClothes = typeof s.msgClothes === "string" ? s.msgClothes : st.msgClothes; st.msgRestraint = typeof s.msgRestraint === "string" ? s.msgRestraint : st.msgRestraint; d.kickCount = (raw.kickCount && typeof raw.kickCount === "object") ? raw.kickCount : {}; d.banCount = (raw.banCount && typeof raw.banCount === "object") ? raw.banCount : Object.assign({}, d.kickCount); d.log = Array.isArray(raw.log) ? raw.log.slice(0, RG_LOG_CAP) : []; } return d; } const RGStore = { state: RGDefaultState(), accountNum: null, saveTimer: null, baseKey() { const num = this.accountNum != null ? this.accountNum : RGCurrentNum(); return num != null ? (RG_STORE_KEY + ":" + num) : null; }, load() { this.accountNum = RGCurrentNum(); const key = this.baseKey(); let raw = null; if (key) raw = RGStorage.get(key); let parsed = null; if (raw) { try { parsed = JSON.parse(raw); } catch (e) { parsed = null; } } this.state = RGNormalizeState(parsed); return this.state; }, ensureAccount(num) { if (Number.isInteger(num) && num > 0 && this.accountNum !== num) { this.accountNum = num; this.load(); } else if (this.accountNum == null) { this.load(); } }, save() { if (this.saveTimer) clearTimeout(this.saveTimer); this.saveTimer = setTimeout(() => { this.saveTimer = null; try { const key = this.baseKey(); if (!key) return; let existing = null; const raw = RGStorage.get(key); if (raw) { try { existing = JSON.parse(raw); } catch (e) { existing = null; } } const merged = RGMergeState(existing, this.state); this.state = merged; RGStorage.set(key, JSON.stringify(merged)); } catch (e) { RGErr("保存失败", e); } }, RG_SAVE_DEBOUNCE_MS); }, flush() { if (this.saveTimer) { clearTimeout(this.saveTimer); this.saveTimer = null; } try { const key = this.baseKey(); if (!key) return; RGStorage.set(key, JSON.stringify(this.state)); } catch (e) { RGErr("flush 失败", e); } }, }; function RGMergeState(existing, mine) { const m = RGNormalizeState(existing); m.settings = mine.settings; const seen = {}; const merged = []; for (const e of (mine.log || [])) { const k = e.t + ":" + e.num; if (!seen[k]) { seen[k] = true; merged.push(e); } } for (const e of (m.log || [])) { const k = e.t + ":" + e.num; if (!seen[k]) { seen[k] = true; merged.push(e); } } merged.sort((a, b) => b.t - a.t); m.log = merged.slice(0, RG_LOG_CAP); for (const k in mine.kickCount) { const a = Number(m.kickCount[k]) || 0; const b = Number(mine.kickCount[k]) || 0; m.kickCount[k] = a > b ? a : b; } for (const k in mine.banCount) { const a = Number(m.banCount[k]) || 0; const b = Number(mine.banCount[k]) || 0; m.banCount[k] = a > b ? a : b; } return m; } const RGState = { mod: null, suspects: new Map(), }; function RGIsAdmin() { try { return typeof ChatRoomPlayerIsAdmin === "function" && ChatRoomPlayerIsAdmin(); } catch (e) { return false; } } function RGIsRelatedToRoom(num) { try { const members = []; if (typeof Player !== "undefined" && Player && Player.MemberNumber !== num) members.push(Player); if (typeof ChatRoomCharacter !== "undefined" && Array.isArray(ChatRoomCharacter)) { for (const c of ChatRoomCharacter) { if (c && c.MemberNumber !== num) members.push(c); } } for (const C of members) { try { if (typeof C.OwnerNumber === "function" && C.OwnerNumber() === num) return true; } catch (e) {   } try { if (typeof C.GetLoversNumbers === "function") { const lovers = C.GetLoversNumbers(true); if (Array.isArray(lovers) && lovers.indexOf(num) >= 0) return true; } } catch (e) {   } } } catch (e) {   } return false; } function RGIsProtected(num) { try { const me = RGCurrentNum(); if (me != null && num === me) return true; if (typeof ChatRoomData !== "undefined" && ChatRoomData) { if (Array.isArray(ChatRoomData.Admin) && ChatRoomData.Admin.includes(num)) return true; if (Array.isArray(ChatRoomData.Whitelist) && ChatRoomData.Whitelist.includes(num)) return true; } if (RGIsRelatedToRoom(num)) return true; } catch (e) {   } return false; } function RGColorKey(c) { if (c == null || c === "Default") return "default"; return JSON.stringify(c); } function RGItemViewOf(C, group) { try { if (C && Array.isArray(C.Appearance)) { const it = C.Appearance.find(i => i && i.Asset && i.Asset.Group && i.Asset.Group.Name === group); if (it) { return { name: it.Asset ? it.Asset.Name : undefined, color: RGColorKey(it.Color), difficulty: (it.Difficulty != null && it.Asset && it.Asset.Difficulty != null) ? (it.Difficulty - it.Asset.Difficulty) : null, }; } } } catch (e) {   } return null; } function RGItemViewFromData(item) { return { name: item.Name, color: RGColorKey(item.Color), difficulty: (item.Difficulty != null) ? item.Difficulty : null, }; } function RGClassifyChange(before, after, group) { const oldName = before ? before.name : undefined; const newName = after ? after.name : undefined; let change = "other"; if (newName !== oldName) { if (newName == null) change = "remove"; else if (oldName == null) change = "add"; else change = "replace"; } else if (before && after) { if (before.color !== after.color) change = "color"; else if (before.difficulty != null && after.difficulty != null && before.difficulty !== after.difficulty) change = "tighten"; } let kind = "clothes"; if ((change === "add" || change === "replace") && newName != null) { try { if (typeof AssetGet === "function") { const a = AssetGet("Female3DCG", group, newName); if (a && a.IsRestraint) kind = "restraint"; } } catch (e) {   } } return { kind, change }; } function RGComputeConds(s) { const st = RGStore.state.settings; s.c1 = s.hasCreation && s.ageDays < st.minAgeDays; return s; } function RGTriggeredKeys(s) { const st = RGStore.state.settings; const keys = []; if (st.condAge && s.c1) keys.push("age"); if (st.condClothes && s.c2) keys.push("clothes"); if (st.condRestraint && s.c3) keys.push("restraint"); return keys; } function RGCondsTrigger(s) { const st = RGStore.state.settings; const enabled = []; if (st.condAge) enabled.push(!!s.c1); if (st.condClothes) enabled.push(!!s.c2); if (st.condRestraint) enabled.push(!!s.c3); if (enabled.length === 0) return false; if (st.combine === "all") return enabled.every(Boolean); if (st.combine === "two") { let n = 0; for (const v of enabled) if (v) n++; return n >= 2; } return enabled.some(Boolean); } function RGMaybeTrigger(s) { if (!s || s.kicked || s.kickPending) return; if (!RGStore.state.settings.enabled) return; if (!RGIsAdmin()) return; RGComputeConds(s); if (RGCondsTrigger(s)) RGKickNow(s); } function RGDoAdmin(action, num) { if (!RGIsAdmin()) return false; try { ServerSend("ChatRoomAdmin", { MemberNumber: num, Action: action }); return true; } catch (e) { RGErr("管理命令失败", action, num, e); return false; } } function RGNotifySendRaw(text) { try { if (typeof ChatRoomSendChatMessage === "function") { ChatRoomSendChatMessage(text); } else { ServerSend("ChatRoomChat", { Content: text, Type: "Chat", Dictionary: [] }); } } catch (e) { RGErr("发送提示消息失败", e); } } function RGNotifySend(s, key) { const st = RGStore.state.settings; const map = { age: "msgAge", clothes: "msgClothes", restraint: "msgRestraint" }; const tmpl = st[map[key]]; if (!tmpl) return; const label = s.nick || s.name || ("#" + s.num); RGNotifySendRaw(tmpl.split("{0}").join(label)); } function RGNotifySendAll(s, keys) { for (const k of keys) RGNotifySend(s, k); } function RGKickNow(s) { if (s.kicked || s.kickPending) return; const st = RGStore.state.settings; const keys = RGTriggeredKeys(s); s.kickPending = true; if (st.notifyBeforeKick) { const msgMs = Math.max(0, Number(st.msgDelaySeconds) || 0) * 1000; const kickMs = Math.max(0, Number(st.kickDelaySeconds) || 0) * 1000; setTimeout(() => RGNotifySendAll(s, keys), msgMs); setTimeout(() => RGFinishKick(s, keys), kickMs); } else { RGFinishKick(s, keys); } } function RGFinishKick(s, keys) { s.kickPending = false; if (RGState.suspects.get(s.num) !== s) return; s.kicked = true; RGState.suspects.delete(s.num); const state = RGStore.state; const entry = { t: RGNow(), num: s.num, name: s.name || "", nick: s.nick || "", reason: keys.join(","), actions: s.actions.slice(0, RG_ACTION_CAP), banned: false, }; state.log.unshift(entry); if (state.log.length > RG_LOG_CAP) state.log.length = RG_LOG_CAP; state.kickCount[s.num] = (Number(state.kickCount[s.num]) || 0) + 1; state.banCount[s.num] = (Number(state.banCount[s.num]) || 0) + 1; const bc = state.banCount[s.num]; const willBan = state.settings.banAfterKicks > 0 && bc >= state.settings.banAfterKicks; entry.banned = willBan; RGStore.save(); if (willBan) RGDoAdmin("Ban", s.num); RGDoAdmin("Kick", s.num); const label = RGDisplayName(s.num); if (willBan) { RGToast(RGT("toastBanned", label, s.num)); } else { RGToast(RGT("toastKicked", label, s.num, RGReasonLabel(entry.reason))); } RGUI.renderLog(); RGUI.renderStatus(); } function RGOnJoin(charData, sourceNum) { const num = (charData && Number.isInteger(charData.MemberNumber)) ? charData.MemberNumber : sourceNum; if (!Number.isInteger(num) || num < 1) return; if (!RGIsAdmin()) return; if (RGIsProtected(num)) return; if (!RGStore.state.settings.enabled) return; const creation = (charData && typeof charData.Creation === "number" && isFinite(charData.Creation) && charData.Creation > 0) ? charData.Creation : null; const hasCreation = creation != null; const ageDays = hasCreation ? ((RGNow() - creation) / RG_DAY_MS) : null; const c = RGCharOf(num); const name = (charData && typeof charData.Name === "string") ? charData.Name : (c ? c.Name : ""); const nick = (charData && typeof charData.Nickname === "string") ? charData.Nickname : (c ? c.Nickname : ""); const s = { num, name: name || "", nick: nick || "", hasCreation, ageDays, joinedAt: RGNow(), spokenCount: 0, c1: false, c2: false, c3: false, kicked: false, kickPending: false, actions: [], }; RGComputeConds(s); RGState.suspects.set(num, s); RGMaybeTrigger(s); } function RGOnMessage(data) { if (!data || typeof data.Sender !== "number") return; if (RG_SPEECH_TYPES.indexOf(data.Type) < 0) return; const s = RGState.suspects.get(data.Sender); if (!s) return; s.spokenCount = (Number(s.spokenCount) || 0) + 1; } function RGSpokenLimit(kind) { const st = RGStore.state.settings; return (kind === "restraint") ? st.restraintMaxSpoken : st.clothesMaxSpoken; } function RGOnItemChange(source, target, group, before, after) { const s = RGState.suspects.get(source); if (!s || s.kicked) return; if (source === target) return; const cls = RGClassifyChange(before, after, group); if ((Number(s.spokenCount) || 0) >= RGSpokenLimit(cls.kind)) return; const itemName = cls.change === "remove" ? (before ? before.name : null) : (after ? after.name : null); if (cls.kind === "restraint") s.c3 = true; else s.c2 = true; s.actions.push({ t: RGNow(), target, kind: cls.kind, change: cls.change, group, item: itemName || null }); if (s.actions.length > RG_ACTION_CAP) s.actions.shift(); RGMaybeTrigger(s); } function RGAppView(C) { const m = {}; try { if (C && Array.isArray(C.Appearance)) { for (const it of C.Appearance) { if (it && it.Asset && it.Asset.Group) { const g = it.Asset.Group.Name; m[g] = { name: it.Asset.Name, color: RGColorKey(it.Color), difficulty: (it.Difficulty != null && it.Asset && it.Asset.Difficulty != null) ? (it.Difficulty - it.Asset.Difficulty) : null, }; } } } } catch (e) {   } return m; } function RGOnFullChange(actor, victim, before, after) { const s = RGState.suspects.get(actor); if (!s || s.kicked) return; if (actor === victim) return; const groups = {}; for (const g in before) groups[g] = true; for (const g in after) groups[g] = true; const changes = []; for (const g in groups) { const b = before[g] || null; const a = after[g] || null; if (JSON.stringify(b) !== JSON.stringify(a)) changes.push(g); } if (!changes.length) return; for (const g of changes) { const b = before[g] || null; const a = after[g] || null; const cls = RGClassifyChange(b, a, g); if ((Number(s.spokenCount) || 0) >= RGSpokenLimit(cls.kind)) continue; const itemName = cls.change === "remove" ? (b ? b.name : null) : (a ? a.name : null); if (cls.kind === "restraint") s.c3 = true; else s.c2 = true; s.actions.push({ t: RGNow(), target: victim, kind: cls.kind, change: cls.change, group: g, item: itemName || null }); if (s.actions.length > RG_ACTION_CAP) s.actions.shift(); } RGMaybeTrigger(s); } function RGInstallHooks(mod) { const safe = (fn) => (...args) => { try { return fn(...args); } catch (e) { RGErr(e); } }; try { mod.hookFunction("ChatRoomSyncMemberJoin", 10, (args, next) => { const res = next(args); safe(() => { const data = args[0]; if (data && data.Character) RGOnJoin(data.Character, data.SourceMemberNumber); })(); return res; }); } catch (e) { RGErr("hook ChatRoomSyncMemberJoin 失败", e); } try { mod.hookFunction("ChatRoomSyncMemberLeave", 10, (args, next) => { const res = next(args); safe(() => { const data = args[0]; if (data && Number.isInteger(data.SourceMemberNumber)) RGState.suspects.delete(data.SourceMemberNumber); })(); return res; }); } catch (e) { RGErr("hook ChatRoomSyncMemberLeave 失败", e); } try { mod.hookFunction("ChatRoomMessage", 10, (args, next) => { const res = next(args); safe(() => RGOnMessage(args[0]))(); return res; }); } catch (e) { RGErr("hook ChatRoomMessage 失败", e); } try { mod.hookFunction("ChatRoomSyncItem", 10, (args, next) => { const data = args[0]; let before = null; if (data && data.Item && Number.isInteger(data.Item.Target) && typeof data.Item.Group === "string") { const tc = RGCharOf(data.Item.Target); before = tc ? RGItemViewOf(tc, data.Item.Group) : null; } const res = next(args); safe(() => { if (data && data.Item && Number.isInteger(data.Source) && Number.isInteger(data.Item.Target)) { RGOnItemChange(data.Source, data.Item.Target, data.Item.Group, before, RGItemViewFromData(data.Item)); } })(); return res; }); } catch (e) { RGErr("hook ChatRoomSyncItem 失败", e); } try { mod.hookFunction("CharacterOnlineRefresh", 10, (args, next) => { const Char = args[0]; const actor = args[2]; const watch = Number.isInteger(actor) && Char && Number.isInteger(Char.MemberNumber) && RGState.suspects.has(actor); const before = watch ? RGAppView(Char) : null; const res = next(args); safe(() => { if (!watch) return; const after = RGAppView(Char); RGOnFullChange(actor, Char.MemberNumber, before, after); })(); return res; }); } catch (e) { RGErr("hook CharacterOnlineRefresh 失败", e); } try { mod.hookFunction("ChatRoomLeave", 10, (args, next) => { const res = next(args); safe(() => { RGState.suspects.clear(); })(); return res; }); } catch (e) { RGErr("hook ChatRoomLeave 失败", e); } try { mod.hookFunction("ChatRoomSendChat", 10, (args, next) => { const input = typeof document !== "undefined" ? document.getElementById("InputChat") : null; const raw = input ? input.value : ""; const t = (raw || "").trim().toLowerCase(); if (t === "/roomguard" || t === "/rg" || t === "/守卫" || t === "/房管") { RGInputClear(input); safe(RoomGuardToggle)(); return; } return next(args); }); } catch (e) { RGErr("hook ChatRoomSendChat 失败", e); } } function RGInputClear(input) { if (!input) return; input.value = ""; try { if (typeof InputEvent !== "undefined") input.dispatchEvent(new InputEvent("input", { bubbles: true })); } catch (e) {   } } const RGText = { zh: { title: "RoomGuard 房间守卫", cmd: "聊天室输入 /rg 或 /守卫 开关本窗", settingsHeader: "设置", enabled: "启用守卫", enabledTitle: "总开关：关闭后守卫不检查、不踢人（日志与设置仍可查看）", condAge: "账号年龄过小（加入不足设定天数）", condAgeTitle: "对方加入 Bondage Club 的时间不足设定天数即触发", condClothes: "未说话就改别人的衣服", condClothesTitle: "进房后一句话没说就开始改其他玩家的外观/服装即触发", condRestraint: "未说话就给别人上束缚道具", condRestraintTitle: "进房后一句话没说就开始给其他玩家穿戴束缚类道具即触发", clothesMaxSpoken: "改衣服：说话少于多少句触发", restraintMaxSpoken: "上束缚：说话少于多少句触发", spokenHint: "提示：设为 1 = 一句话都不说就动手；设为 2 = 说一句话就动手；设为 3 = 说两句话就动手，以此类推。", minAgeDays: "账号年龄阈值（天）", minAgeDaysTitle: "对方账号创建至今不足此天数，视为「新号」", combine: "多条件逻辑", combineAny: "满足其一（任一条件成立即踢）", combineTwo: "满足其二（任意两条条件成立即踢）", combineAll: "同时满足（全部勾选条件都成立才踢）", banAfterKicks: "自动拉黑阈值（踢出次数，0=关闭）", banAfterKicksTitle: "同一玩家被本守卫踢出达到此次数后，自动加入房间黑名单", notifyBeforeKick: "踢出前发送提示消息", notifyBeforeKickTitle: "勾选：先按各条件发送对应的提示消息，3 秒后再踢；不勾选：直接踢", msgAgeLabel: "年龄条件提示消息", msgClothesLabel: "改衣服条件提示消息", msgRestraintLabel: "上束缚条件提示消息", msgPlaceholder: "消息里可用 {0} 表示对方昵称/名字", msgDelaySeconds: "消息延迟（秒）", kickDelaySeconds: "踢出延迟（秒）", logSearchPh: "搜索昵称/名字/数字ID", statusAdmin: "● 你是房间管理员：守卫生效，会检查新加入的玩家", statusNotAdmin: "○ 你不是房间管理员：守卫不会踢人（仅可查看/修改设置与日志）", statusDisabled: "○ 守卫已关闭（主开关未启用）", logHeader: "踢出日志", logBtn: "日志", logEmpty: "暂无记录", logCount: "共 {0} 条", reasonAge: "账号年龄过小", reasonClothes: "未说话即改衣服", reasonRestraint: "未说话即上束缚道具", kindClothes: "改衣服", kindRestraint: "上束缚道具", actionRemoved: "移除", changeAdd: "新增", changeRemove: "移除", changeReplace: "更换", changeColor: "改色", changeTighten: "松紧", changeOther: "其他", bannedBadge: "已拉黑", kickedCount: "累计踢出 {0} 次", remainToBan: "还剩 {0} 次拉黑", resetCountBtn: "归零计数", resetConfirmBtn: "再次点击归零", expandAll: "展开全部 {0} 条", collapse: "收起", exportBtn: "导出日志", clearBtn: "清空日志", clearConfirm: "确认清空", langBtn: "EN", minimizeTitle: "最小化（再点标题栏恢复）", closeTitle: "关闭浮窗", resizeTitle: "拖动拉伸窗口", dotTitle: "RoomGuard 房间守卫", toastOpened: "RoomGuard 已打开：{0}", toastKicked: "已踢出 {0}（#{1}）：{2}", toastBanned: "已踢出并加入房间黑名单：{0}（#{1}）", toastCleared: "已清空全部踢出日志", toastExportClip: "日志已复制到剪贴板，并下载了备份文件", toastExportFile: "备份文件已下载（剪贴板不可用）", toastExportFail: "导出失败：{0}", toastNotAdmin: "你不是房间管理员，无法踢人", toastProtected: "跳过受保护玩家：{0}（管理员/白名单/自己）", }, en: { title: "RoomGuard", cmd: "Type /rg or /roomguard in chat to toggle", settingsHeader: "Settings", enabled: "Enable guard", enabledTitle: "Master switch: when off, guard won't check or kick anyone", condAge: "Account too new (joined less than N days)", condAgeTitle: "Trigger when the player joined Bondage Club less than N days ago", condClothes: "Changed others' clothes without speaking", condClothesTitle: "Trigger when they modify another player's appearance/clothes before saying a word", condRestraint: "Applied restraints without speaking", condRestraintTitle: "Trigger when they put restraint items on another player before saying a word", clothesMaxSpoken: "Clothes: trigger if spoken fewer than N", restraintMaxSpoken: "Restraint: trigger if spoken fewer than N", spokenHint: "Hint: 1 = act without saying a word; 2 = act after one message; 3 = act after two messages, and so on.", minAgeDays: "Account age threshold (days)", minAgeDaysTitle: "Accounts younger than this many days count as 'new'", combine: "Combine logic", combineAny: "Any (trigger if ANY enabled condition is met)", combineTwo: "Any two (trigger if ANY TWO conditions are met)", combineAll: "All (trigger only if ALL enabled conditions are met)", banAfterKicks: "Auto-ban after kicks (0 = off)", banAfterKicksTitle: "After kicking the same player this many times, add them to the room ban list", notifyBeforeKick: "Send notice before kicking", notifyBeforeKickTitle: "Checked: send each satisfied condition's notice, then kick after 3s; unchecked: kick directly", msgAgeLabel: "Age condition notice", msgClothesLabel: "Clothes condition notice", msgRestraintLabel: "Restraint condition notice", msgPlaceholder: "Use {0} for the player's name", msgDelaySeconds: "Message delay (sec)", kickDelaySeconds: "Kick delay (sec)", logSearchPh: "Search nickname/name/ID", statusAdmin: "● You are a room admin: guard is active for new joiners", statusNotAdmin: "○ You are not a room admin: guard won't kick (settings/log still viewable)", statusDisabled: "○ Guard is disabled (master switch off)", logHeader: "Kick log", logBtn: "Log", logEmpty: "No records", logCount: "{0} entries", reasonAge: "Account too new", reasonClothes: "Changed clothes without speaking", reasonRestraint: "Applied restraints without speaking", kindClothes: "Clothes", kindRestraint: "Restraint", actionRemoved: "Removed", changeAdd: "Add", changeRemove: "Remove", changeReplace: "Replace", changeColor: "Color", changeTighten: "Tighten", changeOther: "Other", bannedBadge: "Banned", kickedCount: "Kicked {0} times total", remainToBan: "{0} more to ban", resetCountBtn: "Reset", resetConfirmBtn: "Click again to reset", expandAll: "Show all {0}", collapse: "Collapse", exportBtn: "Export log", clearBtn: "Clear log", clearConfirm: "Confirm clear", langBtn: "中文", minimizeTitle: "Minimize", closeTitle: "Close", resizeTitle: "Drag to resize", dotTitle: "RoomGuard", toastOpened: "RoomGuard opened: {0}", toastKicked: "Kicked {0} (#{1}): {2}", toastBanned: "Kicked and banned: {0} (#{1})", toastCleared: "Cleared the kick log", toastExportClip: "Log copied to clipboard and downloaded", toastExportFile: "Backup file downloaded (clipboard unavailable)", toastExportFail: "Export failed: {0}", toastNotAdmin: "You are not a room admin, cannot kick", toastProtected: "Skipped protected player: {0} (admin/whitelist/self)", }, }; function RGT(key, ...args) { const dict = RGText[RGUI.lang] || RGText.zh; let s = dict[key] != null ? dict[key] : (RGText.zh[key] != null ? RGText.zh[key] : key); for (let i = 0; i < args.length; i++) { s = s.split("{" + i + "}").join(String(args[i])); } return s; } function RGReasonLabel(reasonKeys) { const map = { age: "reasonAge", clothes: "reasonClothes", restraint: "reasonRestraint" }; const parts = []; const keys = String(reasonKeys || "").split(",").filter(x => x); for (const k of keys) { if (map[k]) parts.push(RGT(map[k])); } return parts.length ? parts.join(" + ") : ""; } function RGChangeLabel(change) { const map = { add: "changeAdd", remove: "changeRemove", replace: "changeReplace", color: "changeColor", tighten: "changeTighten", other: "changeOther" }; return map[change] ? RGT(map[change]) : ""; } const RG_ACCENT = "#2ec4b6"; const RG_BG = "#1b1e2b"; const RG_PANEL = "#232838"; const RG_BORDER = "#3a4160"; const RG_TEXT = "#e8e8f0"; const RG_TEXT_DIM = "#9aa0b5"; const RGUI = { lang: "zh", open: false, logOpen: false, win: null, titleEl: null, settingsEl: null, statusEl: null, logWin: null, logTitleEl: null, logEl: null, dot: null, btnLog: null, btnLang: null, btnExport: null, btnClear: null, msgSection: null, combineRow: null, logSearch: "", logSearchEl: null, minimized: false, clearArmed: false, clearTimer: null, refreshTimer: null, }; function RGStyle(el, obj) { for (const k in obj) el.style[k] = obj[k]; } function RGEl(tag, style, text) { const el = document.createElement(tag); if (style) RGStyle(el, style); if (text !== undefined && text !== null) el.textContent = text; return el; } function RGBigBtn(label, onClick, opts) { opts = opts || {}; const b = RGEl("button", { padding: "10px 16px", fontSize: "15px", fontWeight: "600", fontFamily: "sans-serif", background: opts.bg || RG_BORDER, color: "#ffffff", border: "1px solid #ffffff", borderRadius: "8px", cursor: "pointer", }, label); b.title = opts.title || ""; b.addEventListener("click", (ev) => { if (ev && ev.stopPropagation) ev.stopPropagation(); onClick(ev); }); return b; } function RGSmallBtn(label, onClick, opts) { opts = opts || {}; const b = RGEl("button", { padding: "4px 8px", fontSize: "12px", fontFamily: "sans-serif", background: opts.bg || "#2c3350", color: "#ffffff", border: "1px solid #6a7290", borderRadius: "6px", cursor: "pointer", }, label); b.title = opts.title || ""; b.addEventListener("click", (ev) => { if (ev && ev.stopPropagation) ev.stopPropagation(); onClick(ev); }); return b; } function RGAttachDrag(win, bar, res, onSave) { bar.addEventListener("pointerdown", (ev) => { if (ev.target && ev.target.tagName === "BUTTON") return; const drag = { sx: ev.clientX, sy: ev.clientY, ox: win.offsetLeft, oy: win.offsetTop }; const move = (e) => { win.style.left = (drag.ox + e.clientX - drag.sx) + "px"; win.style.top = (drag.oy + e.clientY - drag.sy) + "px"; }; const up = () => { window.removeEventListener("pointermove", move); window.removeEventListener("pointerup", up); if (onSave) onSave(); }; window.addEventListener("pointermove", move); window.addEventListener("pointerup", up); }); res.addEventListener("pointerdown", (ev) => { ev.preventDefault(); ev.stopPropagation(); const drag = { sx: ev.clientX, sy: ev.clientY, ow: win.offsetWidth, oh: win.offsetHeight }; const move = (e) => { let w = drag.ow + e.clientX - drag.sx; let h = drag.oh + e.clientY - drag.sy; if (w < RG_MIN_WIN_W) w = RG_MIN_WIN_W; if (h < RG_MIN_WIN_H) h = RG_MIN_WIN_H; win.style.width = w + "px"; win.style.height = h + "px"; }; const up = () => { window.removeEventListener("pointermove", move); window.removeEventListener("pointerup", up); if (onSave) onSave(); }; window.addEventListener("pointermove", move); window.addEventListener("pointerup", up); }); } function RGUIDotBuild() { if (RGUI.dot) return; const dot = RGEl("div", { position: "fixed", zIndex: "2147483000", width: "46px", height: "46px", borderRadius: "50%", background: "linear-gradient(135deg, #c0392b, #e74c3c)", display: "flex", alignItems: "center", justifyContent: "center", cursor: "pointer", boxShadow: "0 2px 8px rgba(0,0,0,0.4)", userSelect: "none", right: "16px", bottom: "16px", }); const shield = RGEl("span", { display: "flex", alignItems: "center", justifyContent: "center", width: "22px", height: "22px" }); shield.innerHTML = '<svg viewBox="0 0 24 24" width="22" height="22"><path fill="#ffffff" d="M12 2 L20 5 V11 C20 16 16 20 12 22 C8 20 4 16 4 11 V5 Z"/></svg>'; dot.appendChild(shield); dot.title = RGT("dotTitle"); let saved = null; try { const key = RG_DOT_KEY + ":" + (RGStore.accountNum != null ? RGStore.accountNum : 0); saved = RGStorage.get(key); if (saved) { const p = JSON.parse(saved); if (p && Number.isFinite(Number(p.x)) && Number.isFinite(Number(p.y))) { dot.style.right = "auto"; dot.style.left = p.x + "px"; dot.style.top = p.y + "px"; } } } catch (e) {   } let drag = null; dot.addEventListener("pointerdown", (ev) => { drag = { sx: ev.clientX, sy: ev.clientY, ox: dot.offsetLeft, oy: dot.offsetTop, moved: false }; const move = (e) => { if (!drag) return; const dx = e.clientX - drag.sx, dy = e.clientY - drag.sy; if (Math.abs(dx) > 3 || Math.abs(dy) > 3) drag.moved = true; if (drag.moved) { dot.style.right = "auto"; dot.style.left = (drag.ox + dx) + "px"; dot.style.top = (drag.oy + dy) + "px"; } }; const up = () => { window.removeEventListener("pointermove", move); window.removeEventListener("pointerup", up); if (drag && drag.moved) { try { const key = RG_DOT_KEY + ":" + (RGStore.accountNum != null ? RGStore.accountNum : 0); RGStorage.set(key, JSON.stringify({ x: dot.offsetLeft, y: dot.offsetTop })); } catch (e) {   } } drag = null; }; window.addEventListener("pointermove", move); window.addEventListener("pointerup", up); }); dot.addEventListener("click", () => { if (drag && drag.moved) return; RoomGuardToggle(); }); document.body.appendChild(dot); RGUI.dot = dot; } function RGUIBuild() { if (RGUI.win) return; const win = RGEl("div", { position: "fixed", left: "38%", top: "18%", width: "440px", height: "500px", zIndex: "2147483500", background: RG_BG, border: "2px solid #ffffff", borderRadius: "10px", boxShadow: "0 8px 40px rgba(0,0,0,.6)", display: "none", color: RG_TEXT, fontFamily: "sans-serif", fontSize: "14px", overflow: "hidden", flexDirection: "column", }); const bar = RGEl("div", { position: "relative", padding: "8px 12px", background: "#141826", borderBottom: "1px solid " + RG_BORDER, cursor: "move", userSelect: "none", }); const left = RGEl("div", { display: "flex", alignItems: "center", gap: "8px", paddingRight: "80px" }); left.appendChild(RGEl("span", { color: RG_ACCENT, fontSize: "18px" }, "🛡")); const title = RGEl("span", { fontWeight: "700", fontSize: "15px" }, RGT("title")); left.appendChild(title); bar.appendChild(left); const btnMin = RGSmallBtn("—", () => { RGUI.minimized = !RGUI.minimized; RGUIRefresh(); }, { title: RGT("minimizeTitle") }); const btnClose = RGSmallBtn("✕", RoomGuardClose, { title: RGT("closeTitle") }); const right = RGEl("div", { position: "absolute", right: "10px", top: "50%", transform: "translateY(-50%)", display: "flex", alignItems: "center", gap: "8px" }); right.appendChild(btnMin); right.appendChild(btnClose); bar.appendChild(right); const toolbar = RGEl("div", { display: "flex", flexWrap: "wrap", gap: "8px", alignItems: "center", padding: "8px 12px", background: "#20243a", borderBottom: "1px solid " + RG_BORDER, }); const btnLog = RGBigBtn(RGT("logBtn"), RGUILogWinToggle, { bg: "#2c7a70" }); const btnLang = RGBigBtn(RGT("langBtn"), RGUILangToggle); toolbar.appendChild(btnLog); toolbar.appendChild(btnLang); RGUI.btnLog = btnLog; RGUI.btnLang = btnLang; const body = RGEl("div", { flex: "1", overflow: "auto", padding: "12px", background: RG_BG }); const settings = RGEl("div"); const status = RGEl("div", { margin: "12px 0 0", padding: "10px 12px", borderRadius: "8px", background: RG_PANEL, border: "1px solid " + RG_BORDER, fontSize: "13px", }); body.appendChild(settings); body.appendChild(status); const res = RGEl("div", { position: "absolute", right: "0", bottom: "0", width: "18px", height: "18px", cursor: "se-resize", background: "linear-gradient(135deg, transparent 50%, " + RG_BORDER + " 50%)", }); res.title = RGT("resizeTitle"); win.appendChild(bar); win.appendChild(toolbar); win.appendChild(body); win.appendChild(res); document.body.appendChild(win); RGUI.win = win; RGUI.titleEl = title; RGUI.settingsEl = settings; RGUI.statusEl = status; RGAttachDrag(win, bar, res, RGUISaveWinGeom); RGUIBuildSettings(settings); RGUIRestoreWinGeom(); RGUILogWinBuild(); } function RGUILogWinBuild() { if (RGUI.logWin) return; const win = RGEl("div", { position: "fixed", left: "40%", top: "20%", width: "560px", height: "440px", zIndex: "2147483490", background: RG_BG, border: "2px solid #ffffff", borderRadius: "10px", boxShadow: "0 8px 40px rgba(0,0,0,.6)", display: "none", color: RG_TEXT, fontFamily: "sans-serif", fontSize: "14px", overflow: "hidden", flexDirection: "column", }); const bar = RGEl("div", { position: "relative", padding: "8px 12px", background: "#141826", borderBottom: "1px solid " + RG_BORDER, cursor: "move", userSelect: "none", }); const left = RGEl("div", { display: "flex", alignItems: "center", gap: "8px", paddingRight: "44px" }); left.appendChild(RGEl("span", { color: RG_ACCENT, fontSize: "18px" }, "🛡")); const title = RGEl("span", { fontWeight: "700", fontSize: "15px" }, RGT("logHeader")); left.appendChild(title); bar.appendChild(left); const btnClose = RGSmallBtn("✕", RGUILogWinClose, { title: RGT("closeTitle") }); const right = RGEl("div", { position: "absolute", right: "10px", top: "50%", transform: "translateY(-50%)", display: "flex", alignItems: "center", gap: "8px" }); right.appendChild(btnClose); bar.appendChild(right); const toolbar = RGEl("div", { display: "flex", flexWrap: "wrap", gap: "8px", alignItems: "center", padding: "8px 12px", background: "#20243a", borderBottom: "1px solid " + RG_BORDER, }); const btnExport = RGBigBtn(RGT("exportBtn"), RGUIBackupExport); const btnClear = RGBigBtn(RGT("clearBtn"), (ev) => RGUIArmClear(ev && ev.currentTarget ? ev.currentTarget : null), { bg: "#7a2c3a" }); toolbar.appendChild(btnExport); toolbar.appendChild(btnClear); RGUI.btnExport = btnExport; RGUI.btnClear = btnClear; const searchRow = RGEl("div", { padding: "6px 12px", background: RG_PANEL, borderBottom: "1px solid " + RG_BORDER }); const search = RGEl("input", { width: "100%", boxSizing: "border-box", padding: "6px 8px", fontSize: "13px", borderRadius: "6px", background: "#10141f", color: RG_TEXT, border: "1px solid " + RG_BORDER, fontFamily: "sans-serif", }); search.type = "text"; search.placeholder = RGT("logSearchPh"); search.value = RGUI.logSearch || ""; search.addEventListener("input", () => { RGUI.logSearch = search.value; RGUI.renderLog(); }); searchRow.appendChild(search); RGUI.logSearchEl = search; const log = RGEl("div", { flex: "1", overflow: "auto", padding: "10px" }); const res = RGEl("div", { position: "absolute", right: "0", bottom: "0", width: "18px", height: "18px", cursor: "se-resize", background: "linear-gradient(135deg, transparent 50%, " + RG_BORDER + " 50%)", }); res.title = RGT("resizeTitle"); win.appendChild(bar); win.appendChild(toolbar); win.appendChild(searchRow); win.appendChild(log); win.appendChild(res); document.body.appendChild(win); RGUI.logWin = win; RGUI.logTitleEl = title; RGUI.logEl = log; RGAttachDrag(win, bar, res, RGUILogWinSaveGeom); RGUILogWinRestoreGeom(); } function RGUILogWinShow() { RGUILogWinBuild(); RGUI.logOpen = true; RGUI.logWin.style.display = "flex"; RGUI.renderLog(); } function RGUILogWinClose() { RGUI.logOpen = false; if (RGUI.logWin) RGUI.logWin.style.display = "none"; } function RGUILogWinToggle() { if (RGUI.logOpen) RGUILogWinClose(); else RGUILogWinShow(); } function RGUILangToggle() { RGUI.lang = RGUI.lang === "zh" ? "en" : "zh"; try { RGStorage.set(RG_LANG_KEY, RGUI.lang); } catch (e) {   } if (RGUI.titleEl) RGUI.titleEl.textContent = RGT("title"); if (RGUI.logTitleEl) RGUI.logTitleEl.textContent = RGT("logHeader"); if (RGUI.dot) RGUI.dot.title = RGT("dotTitle"); if (RGUI.btnLog) RGUI.btnLog.textContent = RGT("logBtn"); if (RGUI.btnLang) RGUI.btnLang.textContent = RGT("langBtn"); if (RGUI.btnExport) RGUI.btnExport.textContent = RGT("exportBtn"); if (RGUI.btnClear) RGUI.btnClear.textContent = RGT("clearBtn"); if (RGUI.logSearchEl) RGUI.logSearchEl.placeholder = RGT("logSearchPh"); if (RGUI.settingsEl) RGUIBuildSettings(RGUI.settingsEl); RGUI.renderStatus(); RGUI.renderLog(); } function RGUIBuildSettings(root) { root.innerHTML = ""; const h = RGEl("div", { fontWeight: "700", color: RG_ACCENT, fontSize: "13px", marginBottom: "10px" }, RGT("settingsHeader")); root.appendChild(h); root.appendChild(RGCheckboxRow("rg-set-enabled", RGT("enabled"), "change", (v) => { RGStore.state.settings.enabled = v; RGStore.save(); RGUI.renderStatus(); })); root.appendChild(RGCheckboxRow("rg-set-condAge", RGT("condAge"), "change", (v) => { RGStore.state.settings.condAge = v; RGCondChanged(); })); root.appendChild(RGCheckboxRow("rg-set-condClothes", RGT("condClothes"), "change", (v) => { RGStore.state.settings.condClothes = v; RGCondChanged(); })); root.appendChild(RGCheckboxRow("rg-set-condRestraint", RGT("condRestraint"), "change", (v) => { RGStore.state.settings.condRestraint = v; RGCondChanged(); })); root.appendChild(RGNumberRow("rg-set-minAgeDays", RGT("minAgeDays"), 1, 3650, (v) => { RGStore.state.settings.minAgeDays = v; RGStore.save(); })); const spokenHint = RGEl("div", { color: RG_TEXT_DIM, fontSize: "12px", margin: "2px 0 8px" }, RGT("spokenHint")); root.appendChild(spokenHint); root.appendChild(RGNumberRow("rg-set-clothesMaxSpoken", RGT("clothesMaxSpoken"), 1, 100, (v) => { RGStore.state.settings.clothesMaxSpoken = v; RGStore.save(); })); root.appendChild(RGNumberRow("rg-set-restraintMaxSpoken", RGT("restraintMaxSpoken"), 1, 100, (v) => { RGStore.state.settings.restraintMaxSpoken = v; RGStore.save(); })); RGUI.combineRow = RGEl("div", { marginBottom: "10px", fontSize: "13px", color: RG_TEXT }); root.appendChild(RGUI.combineRow); RGUIRenderCombine(); root.appendChild(RGNumberRow("rg-set-banAfterKicks", RGT("banAfterKicks"), 0, 100, (v) => { RGStore.state.settings.banAfterKicks = v; RGStore.save(); })); root.appendChild(RGCheckboxRow("rg-set-notify", RGT("notifyBeforeKick"), "change", (v) => { RGStore.state.settings.notifyBeforeKick = v; RGStore.save(); RGUI.renderSettings(); }, RGT("notifyBeforeKickTitle"))); RGUI.msgSection = RGEl("div", { marginLeft: "6px", paddingLeft: "8px", borderLeft: "2px solid " + RG_BORDER }); root.appendChild(RGUI.msgSection); const hint = RGEl("div", { color: RG_TEXT_DIM, fontSize: "12px", margin: "2px 0 8px" }, RGT("msgPlaceholder")); RGUI.msgSection.appendChild(hint); RGUI.msgSection.appendChild(RGNumberRowDec("rg-set-msgDelay", RGT("msgDelaySeconds"), 0, 60, (v) => { RGStore.state.settings.msgDelaySeconds = v; RGStore.save(); })); RGUI.msgSection.appendChild(RGNumberRowDec("rg-set-kickDelay", RGT("kickDelaySeconds"), 0, 60, (v) => { RGStore.state.settings.kickDelaySeconds = v; RGStore.save(); })); RGUI.msgSection.appendChild(RGTextRow("rg-set-msgAge", RGT("msgAgeLabel"), (v) => { RGStore.state.settings.msgAge = v; RGStore.save(); })); RGUI.msgSection.appendChild(RGTextRow("rg-set-msgClothes", RGT("msgClothesLabel"), (v) => { RGStore.state.settings.msgClothes = v; RGStore.save(); })); RGUI.msgSection.appendChild(RGTextRow("rg-set-msgRestraint", RGT("msgRestraintLabel"), (v) => { RGStore.state.settings.msgRestraint = v; RGStore.save(); })); RGUI.renderSettings(); } function RGCheckboxRow(id, label, evt, onChange, title) { const row = RGEl("label", { display: "flex", alignItems: "center", gap: "8px", marginBottom: "8px", fontSize: "13px", cursor: "pointer", color: RG_TEXT }); row.title = title || ""; const cb = RGEl("input"); cb.type = "checkbox"; cb.id = id; cb.addEventListener(evt, () => onChange(cb.checked)); row.appendChild(cb); row.appendChild(RGEl("span", null, label)); return row; } function RGNumberRow(id, label, min, max, onChange) { const row = RGEl("div", { display: "flex", alignItems: "center", gap: "8px", marginBottom: "8px", fontSize: "13px", color: RG_TEXT }); const span = RGEl("span", { flex: "1" }, label); const inp = RGEl("input", { width: "72px", padding: "6px 8px", fontSize: "13px", borderRadius: "6px", background: "#10141f", color: RG_TEXT, border: "1px solid " + RG_BORDER, fontFamily: "sans-serif", }); inp.type = "number"; inp.id = id; inp.min = String(min); inp.max = String(max); inp.step = "1"; inp.addEventListener("change", () => { let v = Math.floor(Number(inp.value)); if (!Number.isFinite(v)) v = min; if (v < min) v = min; if (v > max) v = max; inp.value = String(v); onChange(v); }); row.appendChild(span); row.appendChild(inp); return row; } function RGNumberRowDec(id, label, min, max, onChange) { const row = RGEl("div", { display: "flex", alignItems: "center", gap: "8px", marginBottom: "8px", fontSize: "13px", color: RG_TEXT }); const span = RGEl("span", { flex: "1" }, label); const inp = RGEl("input", { width: "72px", padding: "6px 8px", fontSize: "13px", borderRadius: "6px", background: "#10141f", color: RG_TEXT, border: "1px solid " + RG_BORDER, fontFamily: "sans-serif", }); inp.type = "number"; inp.id = id; inp.min = String(min); inp.max = String(max); inp.step = "0.5"; inp.addEventListener("change", () => { let v = Number(inp.value); if (!Number.isFinite(v)) v = min; if (v < min) v = min; if (v > max) v = max; v = Math.round(v * 10) / 10; inp.value = String(v); onChange(v); }); row.appendChild(span); row.appendChild(inp); return row; } function RGRadioRow(name, label, options, onChange) { const row = RGEl("div", { marginBottom: "10px", fontSize: "13px", color: RG_TEXT }); const lbl = RGEl("div", { fontWeight: "700", color: RG_TEXT_DIM, marginBottom: "4px" }, label); row.appendChild(lbl); for (const o of options) { const opt = RGEl("label", { display: "block", margin: "3px 0 3px 8px", cursor: "pointer" }); const r = RGEl("input"); r.type = "radio"; r.name = name; r.value = o.value; r.addEventListener("change", () => { if (r.checked) onChange(o.value); }); opt.appendChild(r); opt.appendChild(RGEl("span", null, " " + o.label)); row.appendChild(opt); } return row; } function RGTextRow(id, label, onChange) { const row = RGEl("div", { marginBottom: "8px", fontSize: "13px", color: RG_TEXT }); const lbl = RGEl("div", { color: RG_TEXT_DIM, marginBottom: "3px" }, label); const inp = RGEl("input", { width: "100%", boxSizing: "border-box", padding: "6px 8px", fontSize: "13px", borderRadius: "6px", background: "#10141f", color: RG_TEXT, border: "1px solid " + RG_BORDER, fontFamily: "sans-serif", }); inp.type = "text"; inp.id = id; inp.addEventListener("change", () => onChange(inp.value)); row.appendChild(lbl); row.appendChild(inp); return row; } function RGUIRenderCombine() { if (!RGUI.combineRow) return; const s = RGStore.state.settings; const all3 = s.condAge && s.condClothes && s.condRestraint; RGUI.combineRow.innerHTML = ""; RGUI.combineRow.appendChild(RGEl("div", { fontWeight: "700", color: RG_TEXT_DIM, marginBottom: "4px" }, RGT("combine"))); const options = [{ value: "any", label: RGT("combineAny") }]; if (all3) options.push({ value: "two", label: RGT("combineTwo") }); options.push({ value: "all", label: RGT("combineAll") }); for (const o of options) { const opt = RGEl("label", { display: "block", margin: "3px 0 3px 8px", cursor: "pointer" }); const r = RGEl("input"); r.type = "radio"; r.name = "rg-set-combine"; r.value = o.value; if (o.value === s.combine) r.checked = true; r.addEventListener("change", () => { if (r.checked) { RGStore.state.settings.combine = o.value; RGStore.save(); } }); opt.appendChild(r); opt.appendChild(RGEl("span", null, " " + o.label)); RGUI.combineRow.appendChild(opt); } } function RGCondChanged() { const s = RGStore.state.settings; if (s.combine === "two" && !(s.condAge && s.condClothes && s.condRestraint)) { s.combine = "any"; } RGStore.save(); RGUI.renderSettings(); RGUI.renderStatus(); } function RGWinGeomGet(key, win) { try { const raw = RGStorage.get(key); if (raw) { const g = JSON.parse(raw); if (g && Number.isFinite(Number(g.w)) && Number.isFinite(Number(g.h)) && Number.isFinite(Number(g.x)) && Number.isFinite(Number(g.y))) { win.style.width = g.w + "px"; win.style.height = g.h + "px"; win.style.left = g.x + "px"; win.style.top = g.y + "px"; } } } catch (e) {   } } function RGWinGeomSet(key, win) { try { RGStorage.set(key, JSON.stringify({ x: win.offsetLeft, y: win.offsetTop, w: win.offsetWidth, h: win.offsetHeight })); } catch (e) {   } } function RGUIRestoreWinGeom() { RGWinGeomGet(RG_WIN_KEY + ":" + (RGStore.accountNum != null ? RGStore.accountNum : 0), RGUI.win); } function RGUISaveWinGeom() { RGWinGeomSet(RG_WIN_KEY + ":" + (RGStore.accountNum != null ? RGStore.accountNum : 0), RGUI.win); } function RGUILogWinRestoreGeom() { RGWinGeomGet(RG_WIN_KEY + "Log:" + (RGStore.accountNum != null ? RGStore.accountNum : 0), RGUI.logWin); } function RGUILogWinSaveGeom() { RGWinGeomSet(RG_WIN_KEY + "Log:" + (RGStore.accountNum != null ? RGStore.accountNum : 0), RGUI.logWin); } RGUI.renderSettings = function () { const s = RGStore.state.settings; const get = (id) => this.settingsEl ? this.settingsEl.querySelector("#" + id) : null; const cb = get("rg-set-enabled"); if (cb) cb.checked = !!s.enabled; const ca = get("rg-set-condAge"); if (ca) ca.checked = !!s.condAge; const cc = get("rg-set-condClothes"); if (cc) cc.checked = !!s.condClothes; const cr = get("rg-set-condRestraint"); if (cr) cr.checked = !!s.condRestraint; const md = get("rg-set-minAgeDays"); if (md) md.value = String(s.minAgeDays); const bk = get("rg-set-banAfterKicks"); if (bk) bk.value = String(s.banAfterKicks); const cm = get("rg-set-clothesMaxSpoken"); if (cm) cm.value = String(s.clothesMaxSpoken); const rm = get("rg-set-restraintMaxSpoken"); if (rm) rm.value = String(s.restraintMaxSpoken); const nf = get("rg-set-notify"); if (nf) nf.checked = !!s.notifyBeforeKick; const mdly = get("rg-set-msgDelay"); if (mdly) mdly.value = String(s.msgDelaySeconds); const kdly = get("rg-set-kickDelay"); if (kdly) kdly.value = String(s.kickDelaySeconds); const ma = get("rg-set-msgAge"); if (ma) ma.value = s.msgAge || ""; const mc = get("rg-set-msgClothes"); if (mc) mc.value = s.msgClothes || ""; const mr = get("rg-set-msgRestraint"); if (mr) mr.value = s.msgRestraint || ""; if (this.msgSection) this.msgSection.style.display = s.notifyBeforeKick ? "" : "none"; RGUIRenderCombine(); }; RGUI.renderStatus = function () { if (!this.statusEl) return; const s = RGStore.state.settings; if (!s.enabled) { this.statusEl.textContent = RGT("statusDisabled"); } else if (RGIsAdmin()) { this.statusEl.textContent = RGT("statusAdmin"); } else { this.statusEl.textContent = RGT("statusNotAdmin"); } }; RGUI.renderLog = function () { if (!this.logEl) return; const q = (this.logSearch || "").trim().toLowerCase(); const groups = new Map(); for (const e of RGStore.state.log) { if (!groups.has(e.num)) groups.set(e.num, { num: e.num, name: e.name || "", nick: e.nick || "", kicks: [] }); groups.get(e.num).kicks.push(e); } const list = []; groups.forEach(g => { if (q) { const hay = (g.nick + " " + g.name + " #" + g.num).toLowerCase(); if (hay.indexOf(q) < 0) return; } list.push(g); }); if (!list.length) { this.logEl.textContent = RGT("logEmpty"); return; } this.logEl.innerHTML = ""; for (const g of list) this.logEl.appendChild(RGLogGroupEl(g)); }; function RGLogGroupEl(g) { const wrap = RGEl("div", { borderBottom: "1px solid " + RG_BORDER, padding: "8px 0", fontSize: "12.5px", color: RG_TEXT }); const head = RGEl("div", { display: "flex", alignItems: "center", gap: "8px", marginBottom: "4px" }); const left = RGEl("div", { display: "flex", flexWrap: "wrap", gap: "6px", alignItems: "center", flex: "1", minWidth: "0" }); left.appendChild(RGEl("span", { fontWeight: "700", color: RG_TEXT }, RGDispWho(g))); const kc = Number(RGStore.state.kickCount[g.num]) || 0; left.appendChild(RGEl("span", { color: RG_TEXT_DIM }, RGT("kickedCount", kc))); const banAfter = Number(RGStore.state.settings.banAfterKicks) || 0; if (banAfter > 0) { const bc = Number(RGStore.state.banCount[g.num]) || 0; const remain = Math.max(0, banAfter - bc); left.appendChild(RGEl("span", { color: RG_TEXT_DIM }, RGT("remainToBan", remain))); } const resetArmed = RGResetArmed === g.num; const resetBtn = RGSmallBtn(resetArmed ? RGT("resetConfirmBtn") : RGT("resetCountBtn"), () => RGResetKickCountClick(g.num)); if (resetArmed) { resetBtn.style.background = "#e74c3c"; resetBtn.style.borderColor = "#ffb3b3"; } head.appendChild(left); head.appendChild(resetBtn); wrap.appendChild(head); for (const e of g.kicks) { const kickLine = RGEl("div", { margin: "3px 0 2px 4px" }); kickLine.appendChild(RGEl("span", { color: RG_TEXT_DIM }, RGFormatTime(e.t))); kickLine.appendChild(RGEl("span", { background: "rgba(231,76,60,.18)", color: "#ff9aa2", padding: "1px 6px", borderRadius: "4px", marginLeft: "6px" }, RGReasonLabel(e.reason))); if (e.banned) kickLine.appendChild(RGEl("span", { background: "#e74c3c", color: "#fff", padding: "1px 6px", borderRadius: "4px", marginLeft: "6px" }, RGT("bannedBadge"))); wrap.appendChild(kickLine); const actions = Array.isArray(e.actions) ? e.actions : []; for (const a of actions) { const line = RGEl("div", { margin: "1px 0 1px 16px" }); line.appendChild(RGEl("span", { color: RG_TEXT_DIM, marginRight: "6px" }, RGFormatTime(a.t))); line.appendChild(RGEl("span", { color: RG_TEXT }, RGLogActionText(a))); wrap.appendChild(line); } } return wrap; } function RGLogActionText(a) { const kind = a.kind === "restraint" ? RGT("kindRestraint") : RGT("kindClothes"); const change = RGChangeLabel(a.change); const groupLabel = RGGroupLabel(a.group); const itemLabel = a.item ? RGItemLabel(a.group, a.item) : ""; let detail = kind; if (change) detail += "·" + change; return RGDisplayName(a.target) + " " + detail + "：" + groupLabel + (itemLabel ? " " + itemLabel : ""); } let RGResetArmed = null; let RGResetTimer = null; function RGResetKickCountClick(num) { if (!Number.isInteger(num)) return; if (RGResetArmed === num) { RGResetArmed = null; if (RGResetTimer) { clearTimeout(RGResetTimer); RGResetTimer = null; } RGStore.state.banCount[num] = 0; RGStore.flush(); RGUI.renderLog(); return; } RGResetArmed = num; if (RGResetTimer) clearTimeout(RGResetTimer); RGResetTimer = setTimeout(() => { RGResetArmed = null; RGUI.renderLog(); }, 3000); RGUI.renderLog(); } function RGFormatTime(t) { try { const d = new Date(t); const p = (n) => (n < 10 ? "0" + n : "" + n); return p(d.getMonth() + 1) + "-" + p(d.getDate()) + " " + p(d.getHours()) + ":" + p(d.getMinutes()); } catch (e) { return ""; } } function RGDispWho(e) { const parts = []; if (e.nick) parts.push(e.nick); if (e.name && e.name !== e.nick) parts.push(e.name); parts.push("#" + e.num); return parts.join(" "); } function RGUIRefresh() { if (!RGUI.win) return; if (RGUI.minimized) { for (const c of RGUI.win.children) { if (c !== RGUI.win.firstChild) c.style.display = "none"; } return; } for (const c of RGUI.win.children) c.style.display = ""; RGUI.renderSettings(); RGUI.renderStatus(); } function RoomGuardOpen() { RGStore.load(); RGUIBuild(); RGUI.open = true; RGUI.minimized = false; RGUI.win.style.display = "flex"; RGUIRefresh(); RGToast(RGT("toastOpened", RGIsAdmin() ? RGT("statusAdmin") : RGT("statusNotAdmin"))); if (RGUI.refreshTimer) clearInterval(RGUI.refreshTimer); RGUI.refreshTimer = setInterval(() => { if (!RGUI.open && !RGUI.logOpen) return; RGUI.renderStatus(); if (RGUI.logOpen) RGUI.renderLog(); }, 1500); } function RoomGuardClose() { RGUI.open = false; if (RGUI.win) RGUI.win.style.display = "none"; RGUILogWinClose(); if (RGUI.refreshTimer) { clearInterval(RGUI.refreshTimer); RGUI.refreshTimer = null; } if (RGUI.clearTimer) { clearTimeout(RGUI.clearTimer); RGUI.clearTimer = null; } RGUI.clearArmed = false; } function RoomGuardToggle() { if (RGUI.open) RoomGuardClose(); else RoomGuardOpen(); } function RGUIArmClear(btn) { if (!RGUI.clearArmed) { RGUI.clearArmed = true; if (btn) { btn._origBg = btn.style.background; btn._origBorder = btn.style.borderColor; btn.textContent = RGT("clearConfirm"); btn.style.background = "#e74c3c"; btn.style.borderColor = "#ffb3b3"; } RGUI.clearTimer = setTimeout(() => { RGUI.clearArmed = false; if (btn) { btn.textContent = RGT("clearBtn"); btn.style.background = btn._origBg || ""; btn.style.borderColor = btn._origBorder || ""; } }, 3000); return; } RGUI.clearArmed = false; if (RGUI.clearTimer) { clearTimeout(RGUI.clearTimer); RGUI.clearTimer = null; } if (btn) { btn.textContent = RGT("clearBtn"); btn.style.background = btn._origBg || ""; btn.style.borderColor = btn._origBorder || ""; } RGStore.state.log = []; RGStore.flush(); RGUI.renderLog(); RGToast(RGT("toastCleared")); } function RGUIBackupExport() { const data = { mod: "RoomGuard", version: RG_VERSION, exportedAt: RGNow(), account: RGStore.accountNum, settings: RGStore.state.settings, log: RGStore.state.log, }; const json = JSON.stringify(data, null, 2); let clipOk = false; try { if (typeof navigator !== "undefined" && navigator.clipboard && navigator.clipboard.writeText) { navigator.clipboard.writeText(json); clipOk = true; } } catch (e) {   } try { const blob = new Blob([json], { type: "text/plain;charset=utf-8" }); const url = URL.createObjectURL(blob); const a = document.createElement("a"); a.href = url; a.download = "RoomGuard-log-" + RGDateStamp() + ".txt"; document.body.appendChild(a); a.click(); document.body.removeChild(a); URL.revokeObjectURL(url); } catch (e) { RGErr("导出下载失败", e); } RGToast(clipOk ? RGT("toastExportClip") : RGT("toastExportFile")); } function RGDateStamp() { const d = new Date(); const p = (n) => (n < 10 ? "0" + n : "" + n); return d.getFullYear() + p(d.getMonth() + 1) + p(d.getDate()); } let RGToastEl = null; function RGToast(msg) { try { if (!RGToastEl) { RGToastEl = RGEl("div", { position: "fixed", left: "16px", top: "16px", zIndex: "2147483600", background: "#22283a", border: "1px solid " + RG_ACCENT, color: "#d7f7f2", borderRadius: "8px", padding: "10px 16px", fontSize: "14px", fontFamily: "sans-serif", display: "none", maxWidth: "420px", pointerEvents: "none", }); document.body.appendChild(RGToastEl); } RGToastEl.textContent = msg; RGToastEl.style.display = "block"; if (RGToastEl._t) clearTimeout(RGToastEl._t); RGToastEl._t = setTimeout(() => { RGToastEl.style.display = "none"; }, 2600); } catch (e) {   } } function RGKeyHandler(ev) { if (ev.key !== "Escape") return; const ae = typeof document !== "undefined" ? document.activeElement : null; if (ae && (ae.tagName === "INPUT" || ae.tagName === "TEXTAREA")) return; if (RGUI.logOpen) RGUILogWinClose(); else if (RGUI.open) RoomGuardClose(); } function RGExposeAPI() { const g = (typeof globalThis !== "undefined") ? globalThis : (typeof window !== "undefined" ? window : null); if (!g) return; g.RoomGuardOpen = RoomGuardOpen; g.RoomGuardClose = RoomGuardClose; g.RoomGuardToggle = RoomGuardToggle; g.RoomGuard = { Open: RoomGuardOpen, Close: RoomGuardClose, Toggle: RoomGuardToggle, IsOpen: () => RGUI.open, IsAdmin: RGIsAdmin, State: () => RGStore.load(), Settings: () => RGStore.state.settings, Log: () => RGStore.state.log, Suspects: () => { const out = []; RGState.suspects.forEach((s, num) => { out.push({ num, name: s.name, nick: s.nick, ageDays: s.ageDays, spoken: s.spoken, c1: s.c1, c2: s.c2, c3: s.c3, actions: s.actions.length }); }); return out; }, Kick: (num) => { if (!Number.isInteger(num) || num < 1) return false; if (RGIsProtected(num)) return false; if (!RGIsAdmin()) { RGToast(RGT("toastNotAdmin")); return false; } return RGDoAdmin("Kick", num); }, Ban: (num) => { if (!Number.isInteger(num) || num < 1) return false; if (RGIsProtected(num)) return false; if (!RGIsAdmin()) { RGToast(RGT("toastNotAdmin")); return false; } return RGDoAdmin("Ban", num); }, Export: () => JSON.stringify({ mod: "RoomGuard", version: RG_VERSION, exportedAt: RGNow(), account: RGStore.accountNum, settings: RGStore.state.settings, log: RGStore.state.log }), ClearLog: () => { RGStore.state.log = []; RGStore.flush(); RGUI.renderLog(); return true; }, }; } function RGMain() { if (typeof window !== "undefined" && window.RoomGuardInstalled) { RGLog("已安装，跳过重复加载"); return; } const tryRegister = (tries) => { if (typeof bcModSdk === "undefined" || !bcModSdk.registerMod) { if (tries > 600) { RGErr("等待 bcModSdk 超时（60 秒），mod 未能加载"); return; } setTimeout(() => tryRegister(tries + 1), 100); return; } try { const mod = bcModSdk.registerMod({ name: "RoomGuard", fullName: "RoomGuard — 房间守卫", version: RG_VERSION, repository: "", }, { allowReplace: true }); RGState.mod = mod; RGStore.load(); RGUI.lang = RGStorage.get(RG_LANG_KEY) === "en" ? "en" : "zh"; RGExposeAPI(); RGInstallHooks(mod); if (typeof window !== "undefined") window.RoomGuardInstalled = true; try { RGUIDotBuild(); } catch (e) { RGErr("小圆点按钮构建失败", e); } if (typeof document !== "undefined") { document.addEventListener("keydown", RGKeyHandler); } RGLog("RoomGuard v" + RG_VERSION + " 已加载。聊天室输入 /rg 或 /守卫 开关浮窗；控制台可用 RoomGuardOpen() / window.RoomGuard"); } catch (e) { RGErr("注册 mod 失败", e); } }; tryRegister(0); } if (typeof window !== "undefined" && typeof document !== "undefined") { RGMain(); } if (typeof module !== "undefined" && module.exports) { module.exports = { RGNow, RGStorage, RGStore, RGDefaultState, RGNormalizeState, RGMergeState, RGCurrentNum, RGCharOf, RGDisplayName, RGGroupLabel, RGItemLabel, RGState, RGIsAdmin, RGIsProtected, RGIsRelatedToRoom, RGComputeConds, RGTriggeredKeys, RGCondsTrigger, RGColorKey, RGItemViewOf, RGItemViewFromData, RGClassifyChange, RGOnJoin, RGOnMessage, RGOnItemChange, RGAppView, RGOnFullChange, RGKickNow, RGFinishKick, RGDoAdmin, RGMaybeTrigger, RGNotifySend, RGNotifySendAll, RGNotifySendRaw, RGResetKickCountClick, RGInstallHooks, RGText, RGT, RGReasonLabel, RGChangeLabel, RGUI, RoomGuardOpen, RoomGuardClose, RoomGuardToggle, RGUIRefresh, RGUIBackupExport, RGUILogWinShow, RGUILogWinClose, RGUILogWinToggle, RGUILangToggle, RGAttachDrag, RGUIRenderCombine, RGCondChanged, RGTextRow, RGNumberRowDec, RG_FORMAT: { RGFormatTime, RGDispWho, RGLogGroupEl, RGLogActionText }, }; } })();
